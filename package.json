{
  "name": "file-sharing",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "start": "(cd frontend && npm start)",
    "postinstall": "(cd backend && npm install) && (cd ../frontend && npm install)"
  }
}
